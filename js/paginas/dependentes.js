/* =========================================================
   Pagina: Dependentes
   Projeto: AMBC-V2
   Descricao: Logica especifica da pagina Dependentes
========================================================= */

const DependentesPage = {
  /**
   * Inicializa a pagina.
   * Chamada automaticamente pelo router apos carregar a view.
   */
  init() {
    console.log('[DependentesPage] Inicializada');
  },

  /**
   * Limpa listeners e recursos ao sair da pagina.
   */
  destroy() {
    console.log('[DependentesPage] Destruida');
  }
};

export default DependentesPage;
