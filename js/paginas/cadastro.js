/* =========================================================
   Pagina: Cadastro
   Projeto: AMBC-V2
   Descricao: Logica especifica da pagina Cadastro
========================================================= */

const CadastroPage = {
  /**
   * Inicializa a pagina.
   * Chamada automaticamente pelo router apos carregar a view.
   */
  init() {
    console.log('[CadastroPage] Inicializada');
  },

  /**
   * Limpa listeners e recursos ao sair da pagina.
   */
  destroy() {
    console.log('[CadastroPage] Destruida');
  }
};

export default CadastroPage;
