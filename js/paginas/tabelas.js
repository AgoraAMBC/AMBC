/* =========================================================
   Pagina: Tabelas
   Projeto: AMBC-V2
   Descricao: Logica especifica da pagina Tabelas
========================================================= */

const TabelasPage = {
  /**
   * Inicializa a pagina.
   * Chamada automaticamente pelo router apos carregar a view.
   */
  init() {
    console.log('[TabelasPage] Inicializada');
  },

  /**
   * Limpa listeners e recursos ao sair da pagina.
   */
  destroy() {
    console.log('[TabelasPage] Destruida');
  }
};

export default TabelasPage;
