/* =========================================================
   Pagina: Dashboard
   Projeto: AMBC-V2
   Descricao: Logica especifica da pagina Dashboard
========================================================= */

const DashboardPage = {
  /**
   * Inicializa a pagina.
   * Chamada automaticamente pelo router apos carregar a view.
   */
  init() {
    console.log('[DashboardPage] Inicializada');
  },

  /**
   * Limpa listeners e recursos ao sair da pagina.
   */
  destroy() {
    console.log('[DashboardPage] Destruida');
  }
};

export default DashboardPage;
